name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
      - run: bun run build:cowork
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/skills/*.zip
          generate_release_notes: true
